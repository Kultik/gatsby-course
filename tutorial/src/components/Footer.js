// Functional component : we would still need to import the footer to our index.js

import React from 'react'

const Footer = () => {
    return (
        <div>
            <h1>Hello from footer</h1>
        </div>
    )
}

export default Footer
